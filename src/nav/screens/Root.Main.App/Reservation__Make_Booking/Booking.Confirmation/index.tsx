import {Header} from '@src/components/Header';
import {SafeAreaView} from '@src/components/SafeAreaWrapper';

export function Confirmation() {
    return (
        <SafeAreaView>
            <Header title="Confirm Reservation" />
        </SafeAreaView>
    );
}
