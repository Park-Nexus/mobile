import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    wrapper: {
        flex: 1,
        paddingHorizontal: 16,
    },
    submitButton: {
        position: 'absolute',
        left: 16,
        right: 16,
    },
});
