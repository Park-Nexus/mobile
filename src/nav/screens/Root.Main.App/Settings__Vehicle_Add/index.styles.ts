import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    wrapper: {
        flex: 1,
        paddingHorizontal: 16,
    },

    formFieldLabel: {
        fontSize: 14,
        fontWeight: '600',
        marginBottom: 4,
    },
    vehicleTypeLabel: {
        fontSize: 14,
        fontWeight: '600',
    },

    saveButton: {
        marginHorizontal: 16,
        marginVertical: 16,
    },
});
